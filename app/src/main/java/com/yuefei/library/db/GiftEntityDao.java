package com.yuefei.library.db;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "GIFT_ENTITY".
*/
public class GiftEntityDao extends AbstractDao<GiftEntity, Long> {

    public static final String TABLENAME = "GIFT_ENTITY";

    /**
     * Properties of entity GiftEntity.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property GiftId = new Property(1, String.class, "giftId", false, "GIFT_ID");
        public final static Property GiftUrl = new Property(2, String.class, "giftUrl", false, "GIFT_URL");
        public final static Property FilePath = new Property(3, String.class, "filePath", false, "FILE_PATH");
        public final static Property FileName = new Property(4, String.class, "fileName", false, "FILE_NAME");
    };


    public GiftEntityDao(DaoConfig config) {
        super(config);
    }
    
    public GiftEntityDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"GIFT_ENTITY\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"GIFT_ID\" TEXT," + // 1: giftId
                "\"GIFT_URL\" TEXT UNIQUE ," + // 2: giftUrl
                "\"FILE_PATH\" TEXT," + // 3: filePath
                "\"FILE_NAME\" TEXT);"); // 4: fileName
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"GIFT_ENTITY\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, GiftEntity entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String giftId = entity.getGiftId();
        if (giftId != null) {
            stmt.bindString(2, giftId);
        }
 
        String giftUrl = entity.getGiftUrl();
        if (giftUrl != null) {
            stmt.bindString(3, giftUrl);
        }
 
        String filePath = entity.getFilePath();
        if (filePath != null) {
            stmt.bindString(4, filePath);
        }
 
        String fileName = entity.getFileName();
        if (fileName != null) {
            stmt.bindString(5, fileName);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, GiftEntity entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String giftId = entity.getGiftId();
        if (giftId != null) {
            stmt.bindString(2, giftId);
        }
 
        String giftUrl = entity.getGiftUrl();
        if (giftUrl != null) {
            stmt.bindString(3, giftUrl);
        }
 
        String filePath = entity.getFilePath();
        if (filePath != null) {
            stmt.bindString(4, filePath);
        }
 
        String fileName = entity.getFileName();
        if (fileName != null) {
            stmt.bindString(5, fileName);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public GiftEntity readEntity(Cursor cursor, int offset) {
        GiftEntity entity = new GiftEntity( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // giftId
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // giftUrl
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // filePath
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4) // fileName
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, GiftEntity entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setGiftId(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setGiftUrl(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setFilePath(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setFileName(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(GiftEntity entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(GiftEntity entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
